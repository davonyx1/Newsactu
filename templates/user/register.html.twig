{% extends "base.html.twig" %}

{% block title %}Inscription - NewsActu{% endblock %}

{% block body %}

<h1 class="my-4 text-center">Inscription</h1>

<div class="rox">
    <div class="col-6 mx-auto">
    {{ form_start(form) }}
    <div class="row">
        <div class="col-6">{{ form_row(form.firstname) }}</div>
        <div class="col-6">{{ form_row(form.lastname) }}</div>
    </div>

    {{ form_end(form)}}
    </div>
</div>

{% endblock %}
